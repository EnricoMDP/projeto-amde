spring:
  application:
    name: gestao-clinica

  datasource:
    url: jdbc:postgresql://localhost:5432/gestao_clinica
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: 8080
